name: Football Telegram Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Python
        run: |
          python3 --version
          echo "GOAL ALERT BOT ONLINE"
